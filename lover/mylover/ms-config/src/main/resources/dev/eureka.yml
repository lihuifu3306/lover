server:
  port: ${lover.service.eureka.port}
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  server:
    waitTimeInMsWhenSyncEmpty: 0
    eviction-interval-timer-in-ms: 2000
  instance:
    hostname: eureka-server
    prefer-ip-address: true